import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: 'column',
        justifyContent: 'flex-start',
        alignItems: 'center',
        margin: 10
    },
    button: {
        backgroundColor: '#f2d2d0',
        borderRadius: 5,
        width: 300,
        height:150,
        alignItems: 'center',
        justifyContent: 'center',
        margin: 10
    },
    text: {
        fontSize: 30,
    }
});